(()=>{'use strict';const $=id=>document.getElementById(id);const state={vehicles:[],modules:[],accessories:[],filter:'all'};const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const normal=s=>String(s??'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();async function json(url,fallback){try{const r=await fetch(url);if(!r.ok)throw Error(r.status);return await r.json()}catch(e){console.warn('V3 data unavailable',url,e);return fallback}}async function init(){const [v,m,a]=await Promise.all([json('./data/vehicles.json',{vehicles:[]}),json('./data/modules.json',{modules:[]}),json('../Honda/Accessories/data/accessories.json',{accessories:[]})]);state.vehicles=v.vehicles||[];state.modules=m.modules||[];state.accessories=a.accessories||a.records||[];wire();coverage();browseAccessories();route(location.hash.slice(1)||'dashboard')}function wire(){document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>route(b.dataset.view));$('quickGo').onclick=()=>{route('search');$('globalInput').value=$('quickSearch').value;search()};$('quickSearch').onkeydown=e=>{if(e.key==='Enter')$('quickGo').click()};$('globalGo').onclick=search;$('globalInput').onkeydown=e=>{if(e.key==='Enter')search()};$('vinGo').onclick=decodeVin;$('vinInput').onkeydown=e=>{if(e.key==='Enter')decodeVin()}}function route(name){document.querySelectorAll('.view').forEach(v=>v.classList.toggle('active',v.id===name));document.querySelectorAll('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===name));const title={dashboard:'Dashboard',vin:'VIN Lookup',search:'Global Search',accessories:'Accessories',fluids:'Fluids',technical:'Technical Resources'}[name]||'Pleiades V3';$('pageTitle').textContent=title;history.replaceState(null,'','#'+name)}function coverage(){const records=[['VIN profiles',state.vehicles.length,'live'],['Hosted accessories',state.accessories.length,state.accessories.length?'live':'partial'],['Connected knowledge modules',state.modules.length,'live'],['Australian specification coverage',state.vehicles.filter(v=>v.engine?.powerKw).length,'research']];$('coverageCards').innerHTML=records.map(([n,c,s])=>`<article class="card"><div class="state ${s}">${s}</div><h3>${esc(n)}</h3><p><strong>${c}</strong> indexed records or modules</p></article>`).join('')}function decodeVin(){const vin=$('vinInput').value.toUpperCase().replace(/[^A-Z0-9]/g,'');$('vinInput').value=vin;const out=$('vehicleResult');if(vin.length!==17){$('vinStatus').textContent='Enter all 17 VIN characters. Manual brand portals remain available when decoding fails.';out.innerHTML='';return}const vehicle=state.vehicles.find(v=>(v.vinPrefixes||[]).some(p=>vin.startsWith(p)));if(!vehicle){$('vinStatus').textContent='VIN fingerprint is not yet confirmed for the Australian market. Nothing has been guessed.';out.innerHTML=`<div class="notice">Use Global Search or the legacy brand portal while this VIN remains under review.</div>`;return}$('vinStatus').textContent='Confirmed VIN fingerprint match.';const e=vehicle.engine||{};const ptw=e.powerKw&&vehicle.kerbWeightKg?(e.powerKw/(vehicle.kerbWeightKg/1000)).toFixed(1):null;out.innerHTML=`<div class="vehicle-title">${vehicle.year} ${esc(vehicle.brand)} ${esc(vehicle.model)} ${esc(vehicle.variant)}</div><div class="facts">${fact('Body',vehicle.body)}${fact('Chassis',vehicle.chassis)}${fact('Transmission',vehicle.transmission)}${fact('Drive',vehicle.drive)}</div><h3>Power and weight</h3><div class="specs">${fact('Engine',e.description||e.code)}${fact('Power',e.powerKw?`${e.powerKw} kW${e.powerRpm?` @ ${e.powerRpm} rpm`:''}`:null)}${fact('Torque',e.torqueNm?`${e.torqueNm} Nm${e.torqueRpm?` @ ${e.torqueRpm} rpm`:''}`:null)}${fact('Kerb weight',vehicle.kerbWeightKg?`${vehicle.kerbWeightKg} kg`:null)}${fact('Power / weight',ptw?`${ptw} kW/tonne`:null)}${fact('0–100 km/h',vehicle.zeroToHundredSec?`${vehicle.zeroToHundredSec} sec`:null)}</div><div class="notice ok">Confidence: ${esc(vehicle.confidence||'confirmed')}</div>`}function fact(k,v){return `<div class="fact"><small>${esc(k)}</small><strong>${esc(v||'Not yet verified')}</strong></div>`}function buildIndex(){const vehicles=state.vehicles.map(v=>({type:'vehicle',title:`${v.year} ${v.brand} ${v.model} ${v.variant}`,detail:`${v.body||''} ${(v.vinPrefixes||[]).join(' ')} ${v.engine?.description||''} ${v.engine?.powerKw||''} kW ${v.engine?.torqueNm||''} Nm`,href:'#vin',payload:v}));const accessories=state.accessories.map(a=>({type:'accessory',title:a.partNumber||a.pn||'Accessory',detail:`${a.description||a.desc||''} ${a.notes||''} ${a.model||''} ${a.variant||''}`,href:'../Honda/Accessories.html',payload:a}));const modules=state.modules.map(m=>({type:m.type||'module',title:m.name,detail:`${m.keywords||''} ${m.status||''}`,href:m.href,payload:m}));return [...vehicles,...accessories,...modules]}function search(){const q=normal($('globalInput').value);const all=buildIndex();const types=['all',...new Set(all.map(x=>x.type))];$('searchFilters').innerHTML=types.map(t=>`<button data-filter="${esc(t)}" class="${state.filter===t?'active':''}">${esc(t)}</button>`).join('');$('searchFilters').querySelectorAll('button').forEach(b=>b.onclick=()=>{state.filter=b.dataset.filter;search()});const terms=q.split(' ').filter(Boolean);const found=all.filter(x=>state.filter==='all'||x.type===state.filter).map(x=>{const hay=normal(`${x.title} ${x.detail}`);const score=terms.reduce((n,t)=>n+(hay.includes(t)?1:0),0);return {...x,score}}).filter(x=>!terms.length||x.score===terms.length).sort((a,b)=>b.score-a.score||a.title.localeCompare(b.title));$('searchResults').innerHTML=found.length?found.slice(0,250).map(x=>`<article class="result-card"><span class="badge">${esc(x.type)}</span><h3>${esc(x.title)}</h3><p class="muted">${esc(x.detail)}</p><a class="action" href="${esc(x.href)}">Open</a></article>`).join(''):'<div class="notice">No hosted V3 record matched. Try a broader term or use the connected legacy portal.</div>'}function browseAccessories(){const rows=state.accessories.slice(0,250);$('accessoryBrowser').innerHTML=rows.length?rows.map(a=>`<article class="result-card"><span class="badge">${esc(a.category||'Accessory')}</span><h3>${esc(a.partNumber||a.pn)}</h3><p>${esc(a.description||a.desc)}</p><p class="muted">${esc(a.notes||'')}</p></article>`).join(''):'<div class="notice">The hosted accessory file is empty or unavailable. The source importer must be run before this section is complete.</div>'}document.addEventListener('DOMContentLoaded',init)})();