# Admin guide

`v4/admin.html` is a safe static admin preview. It validates pasted JSON and downloads import payloads. It never stores tokens client-side and does not overwrite repository data from the browser.
