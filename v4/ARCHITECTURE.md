# Architecture

V4 is a unified static application. Canonical build-time data lives in `v4/data/canonical/pleiades-v4.json`; generated public indexes live in `v4/public/data`. The browser fetches JSON on demand for dashboard sections, global search, VIN decoding and detail cards.
