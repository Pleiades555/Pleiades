# Import guide

Imports must validate first, preserve manual records, merge by stable IDs, create backups, report additions/updates/unchanged/conflicts/rejections, and require explicit destructive approval. Use `legacy-structured-backup.json` as the machine-readable migration backup.
