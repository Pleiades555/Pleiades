# Source policy

Keep source documents separate from structured facts. Unknown values remain unknown. Estimates must be marked `estimated` or `manual-review`; conflicting evidence is retained and flagged rather than resolved silently.
