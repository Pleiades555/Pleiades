# Accessory record schema

Each item may contain: `year`, `model`, `body`, `category`, `partNumber`, `description`, `notes`, `variants`, `frt`, `listPrice`, `fittedPrice`, and `source`.

The live portal filters by year, model, body, and variant/fuel applicability. Empty `variants` means the row applies broadly within the matching year/model/body source.
